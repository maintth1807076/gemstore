@model IEnumerable<GemStore.Models.JewelTypeMst>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JewelleryType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JewelleryType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JewelleryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JewelleryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JewelleryId })
        </td>
    </tr>
}

</table>
